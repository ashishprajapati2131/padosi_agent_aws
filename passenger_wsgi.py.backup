def application(environ, start_response):
    body = b"PASSENGER WSGI IS WORKING"
    start_response(
        "200 OK",
        [
            ("Content-Type", "text/plain"),
            ("Content-Length", str(len(body))),
        ],
    )
    return [body]
